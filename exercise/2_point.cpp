#include <iostream>

#include "../include/point.hpp"

/**
 * !Exercício 1: Encontrar o ponto mais próximo da origem
 *
 * Dada uma lista de pontos no plano cartesiano, seu objetivo é determinar qual
 * o ponto mais próximo da origem (0, 0).
 *
 * !Entrada:
 * A entrada consiste em uma lista de pontos, onde cada ponto é fornecido por
 * suas coordenadas (x, y). O primeiro número da entrada é um inteiro n
 * representando o número de pontos. Em seguida, n linhas seguem com as
 * coordenadas x e y de cada ponto.
 *
 * !Saída:
 * Imprima as coordenadas do ponto mais próximo da origem.
 *
 * !Exemplo de Entrada:
 * 4
 * 1 2
 * 3 4
 * 0 -1
 * -2 -2
 *
 * !Exemplo de Saída:
 * Ponto mais próximo da origem: (0, -1)
 */

int main() {
  /* code */
  return 0;
}
